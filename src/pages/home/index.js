import React from 'react';
import { Layout } from '../../components/layout';

const index = () => {
    return (
        <div>
            <Layout></Layout>
        </div>
    );
};

export default index;